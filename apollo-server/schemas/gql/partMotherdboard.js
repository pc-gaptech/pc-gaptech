const partMotherboard = `
    id: ID
    name: String
    socket: [Socket]
    chipset: [Chipset]
    form_factor: [FormFactor]
    manufacturer: String
    power_draw: Int
    price: Int
    picture_url: String
`;

module.exports = partMotherboard;
